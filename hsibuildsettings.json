{
	"task-order": [
		"build-server",
		"test"
	],
	"tasks": {
		"test": {
			"sync": true,
			"doesFail": true,
			"failText": "One or more of the tests have failed.",
			"commands": [
				"npx jest"
			]
		},
		"build-server": {
			"sync": true,
			"doesFail": true,
			"commands": [
				"npx tsc"
			],
			"failText": "Snowpack compilation failed."
		}
	}
}